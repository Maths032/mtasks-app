import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  bodyContainer: {
    flex: 1,
    width: '100%',
    height: '100%',
    backgroundColor: 'red'
  }
})

export default styles
